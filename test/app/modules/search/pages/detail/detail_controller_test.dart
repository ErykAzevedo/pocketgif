import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:pocketmeme/app/modules/search/search_module.dart';

void main() {
  initModule(SearchModule());
  // DetailController detail;
  //
  setUp(() {
    //     detail = SearchModule.to.get<DetailController>();
  });

  group('DetailController Test', () {
    //   test("First Test", () {
    //     expect(detail, isInstanceOf<DetailController>());
    //   });

    //   test("Set Value", () {
    //     expect(detail.value, equals(0));
    //     detail.increment();
    //     expect(detail.value, equals(1));
    //   });
  });
}
